# UX Polish Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Fix the visible UX/UI leaks found in the live audit without redesigning the site.

**Architecture:** Keep the current visual system. Apply small CSS/content adjustments to gallery, build, home proof timing, and the agent protocol pill.

**Tech Stack:** Next.js app router, CSS modules, existing Node test/build scripts, Playwright browser checks.

---

### Task 1: Gallery Contrast And Mobile Visibility

**Files:**
- Modify: `styles/PhotoGallery.module.css`
- Modify: `styles/MenuItem.module.css`

**Steps:**
1. Improve gallery card contrast so study cards read as interactive, not disabled.
2. On mobile, reduce dead vertical space so the first study card starts inside the first viewport.
3. Keep existing typography and imagery; no new components.
4. Verify `/gallery/` desktop and mobile screenshots show visible cards and no overflow.

### Task 2: Build Page Mobile Gutter

**Files:**
- Modify: `styles/build.module.css`

**Steps:**
1. Fix mobile hero text touching the viewport edge.
2. Keep the existing conversion copy and CTA.
3. Verify `/build/` mobile screenshot has consistent left/right gutter and no overflow.

### Task 3: Home Proof Timing

**Files:**
- Modify: `components/home/HeroSection.module.css`
- Modify: `components/home/ProofConsole.module.css`

**Steps:**
1. Slightly shorten mobile hero/proof spacing so operational proof begins earlier.
2. Do not weaken the terrain hero or remove the first-screen cinematic feel.
3. Verify home mobile still reads cleanly and proof starts near the first viewport.

### Task 4: Protocol Pill Affordance

**Files:**
- Modify: `components/AgentAwareness.module.css`

**Steps:**
1. Make the mobile protocol pill less mysterious without blocking content.
2. Keep it compact; no banner on mobile.
3. Verify the mobile menu still layers above the pill.

### Task 5: Verification

Run:

```bash
npm test
git diff --check
npm run build
```

Then run browser checks for `/`, `/gallery/`, `/build/`, and mobile menu layering.
